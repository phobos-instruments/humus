# Build trees. `make build ARCH=x86_64` gives each arch its own.
engine/build/
engine/build-*/
build/

# Editor and OS scratch.
.DS_Store
.idea/
.vscode/

# Downloaded and converted by the configure step, so a build does not leave
# the checkout dirty.
packs/av/assets/Models/hands/hands.humnet
packs/av/assets/Models/body/body.humnet
